//# include "push_swap.h"

//  NE PEUT PAS FONCTIONNER !!!

/*
int ft_check_empty_array_a(t_array *a)
{
    if (*a->a == NULL)
    {
        write(1, "le tableau a est vide !\n", 24);
        return (0);
    }
    return (1);
}

int ft_check_empty_array_b(t_array *b)
{
    if (*b->b == NULL)
    {
        write(1, "le tableau b est vide !\n", 24);
        return (0);
    }
    return (1);
}

int ft_check_empty_array(t_array *array)
{
    if (*array->array == NULL)
    {
        write(1, "le tableau est vide !\n", 24);
        return (0);
    }
    return (1);
}
*/